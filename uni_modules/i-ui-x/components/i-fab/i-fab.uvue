<template>
  <view
    class="i-fab"
    :style="fabStyle"
    @touchstart.stop="handleTouchStart"
    @touchmove.stop.prevent="handleTouchMove"
    @touchend.stop="handleTouchEnd"
    @click="handleClick"
    @longpress="handleLongPress"
  >
    <slot>
      <view v-if="icon == '+'" class="i-fab__plus">
        <view class="i-fab__plus-line" :style="plusLineStyle"></view>
        <view class="i-fab__plus-line i-fab__plus-line--vertical" :style="plusLineStyle"></view>
      </view>
      <text v-else class="i-fab__default-icon" :style="iconStyle">{{ icon }}</text>
    </slot>
  </view>
</template>

<script setup lang="uts">
import { computed, nextTick, onMounted, ref, watch } from 'vue'

defineOptions({ name: 'i-fab' })

/**
 * Props 说明：依据 DCloud uni-app x floating button 浮球。
 * - duration: 松手后吸附动画时长，单位 ms。
 * - threshold: 左右边界安全距离，单位 px；吸附到两侧时保留这个距离。
 * - thresholdTop: 顶部安全距离，单位 px。
 * - thresholdBottom: 底部安全距离，单位 px。
 * - round: 圆角，支持数字、px、rpx；默认 64，通常配合 50px 尺寸形成圆形。
 * - offset: 当前坐标，支持 v-model:offset；预设 [-1,-1] 右下、[-2,-2] 左下、[-3,-3] 左上、[-4,-4] 右上、[-5,-5] 底部居中。
 * - bgColor: 背景色，支持普通颜色和 linear-gradient 渐变。
 * - width: 浮球宽度，支持数字、px、rpx。
 * - height: 浮球高度，支持数字、px、rpx。
 * - adsorption: 是否开启左右吸附；false 时松手后停留在释放位置。
 * - disabled: 是否禁止拖动。
 * - zIndex: 浮球层级。
 * - icon/color: 本项目兼容默认插槽图标；传入默认 slot 时可完全自定义内容。
 */
const props = defineProps({
  duration: { type: Number, default: 650 },
  threshold: { type: Number, default: 12 },
  thresholdTop: { type: Number, default: 0 },
  thresholdBottom: { type: Number, default: 12 },
  round: { type: [String, Number], default: '64' },
  offset: {
    type: Array,
    default() {
      return [-1, -1]
    },
  },
  bgColor: { type: String, default: '' },
  width: { type: [String, Number], default: '50px' },
  height: { type: [String, Number], default: '50px' },
  adsorption: { type: Boolean, default: true },
  disabled: { type: Boolean, default: false },
  zIndex: { type: Number, default: 87 },
  icon: { type: String, default: '+' },
  color: { type: String, default: '#ffffff' },
})

/**
 * Emits 说明：浮球点击、长按、拖动坐标变化与 v-model 同步。
 * - click: 点击浮球时触发；拖动后的 click 会被过滤。
 * - longpress: 长按浮球超过平台 longpress 阈值时触发。
 * - change: 坐标变化时触发，参数为 [x, y]。
 * - update:offset: 同步当前坐标，支持 v-model:offset。
 */
const emit = defineEmits(['click', 'longpress', 'change', 'update:offset'])


function numericSize(value : any) : number {
  const text = (value).toString()
  const numberValue = parseFloat((text.replace('px', '').replace('rpx', '').replace('%', '')).toString())
  if (isNaN(numberValue)) return 0
  return numberValue
}

function formatSize(value : any) : string {
  const text = (value).toString()
  if (text.indexOf('px') >= 0 || text.indexOf('rpx') >= 0 || text.indexOf('%') >= 0) return text
  return text + 'px'
}

const bgColor = computed(() : string => props.bgColor)
const x = ref(0)
const y = ref(0)
const startClientX = ref(0)
const startClientY = ref(0)
const startX = ref(0)
const startY = ref(0)
const moving = ref(false)
const moved = ref(false)
const windowWidth = ref(375)
const windowHeight = ref(667)

function refreshWindowInfo() : void {
  const info = uni.getWindowInfo()
  windowWidth.value = parseFloat((info.windowWidth).toString())
  windowHeight.value = parseFloat((info.windowHeight).toString())
}

function offsetValue(value : Array<any | null> | null, index : number) : number {
  if (value == null || value.length <= index) return 0
  const item = value[index]
  if (item == null) return 0
  const numberValue = parseFloat(item.toString())
  return isNaN(numberValue) ? 0 : numberValue
}

function leftX() : number {
  return props.threshold
}

function rightX(widthValue : number) : number {
  return windowWidth.value - widthValue - props.threshold
}

function centerX(widthValue : number) : number {
  return (windowWidth.value - widthValue) / 2
}

function topY() : number {
  return props.thresholdTop
}

function bottomY(heightValue : number) : number {
  return windowHeight.value - heightValue - props.thresholdBottom
}

function clampX(value : number, widthValue : number) : number {
  const min = leftX()
  const max = rightX(widthValue)
  if (value < min) return min
  if (value > max) return max
  return value
}

function clampY(value : number, heightValue : number) : number {
  const min = topY()
  const max = bottomY(heightValue)
  if (value < min) return min
  if (value > max) return max
  return value
}

function resolveOffset(value : Array<any | null> | null) : Array<number> {
  const first = offsetValue(value, 0)
  const second = offsetValue(value, 1)
  const widthValue = numericSize(props.width)
  const heightValue = numericSize(props.height)
  if (first == -1 && second == -1) return [rightX(widthValue), bottomY(heightValue)]
  if (first == -2 && second == -2) return [leftX(), bottomY(heightValue)]
  if (first == -3 && second == -3) return [leftX(), topY()]
  if (first == -4 && second == -4) return [rightX(widthValue), topY()]
  if (first == -5 && second == -5) return [centerX(widthValue), bottomY(heightValue)]
  return [clampX(first, widthValue), clampY(second, heightValue)]
}

function applyOffset() : void {
  refreshWindowInfo()
  const next = resolveOffset(props.offset)
  x.value = next[0]
  y.value = next[1]
}

function readTouch(event : UniTouchEvent) : Array<number> {
  if (event.touches.length == 0) return [0, 0]
  const point = event.touches[0]
  return [point.clientX, point.clientY]
}

function emitOffset() : void {
  const value : Array<number> = [Math.round(x.value), Math.round(y.value)]
  emit('change', value)
  emit('update:offset', value)
}

const fabStyle = computed(() : string => {
  const bg = bgColor.value.length > 0 ? bgColor.value : '#1296f7'
  const transition = moving.value ? '0ms' : (props.duration).toString() + 'ms'
  return 'left:' + (Math.round(x.value)).toString() + 'px;top:' +
    (Math.round(y.value)).toString() + 'px;width:' + formatSize(props.width) +
    ';height:' + formatSize(props.height) + ';border-radius:' + formatSize(props.round) +
    ';background:' + bg + ';z-index:' + (props.zIndex).toString() +
    ';transition-duration:' + transition + ';'
})

const iconStyle = computed(() : string => {
  return 'color:' + props.color + ';width:' + formatSize(props.width) +
    ';height:' + formatSize(props.height) + ';line-height:' + formatSize(props.height) + ';'
})

const plusLineStyle = computed(() : string => 'background-color:' + props.color + ';')

function handleTouchStart(event : UniTouchEvent) : void {
  if (props.disabled) return
  refreshWindowInfo()
  const point = readTouch(event)
  startClientX.value = point[0]
  startClientY.value = point[1]
  startX.value = x.value
  startY.value = y.value
  moving.value = true
  moved.value = false
}

function handleTouchMove(event : UniTouchEvent) : void {
  if (props.disabled) return
  const point = readTouch(event)
  const widthValue = numericSize(props.width)
  const heightValue = numericSize(props.height)
  x.value = clampX(startX.value + point[0] - startClientX.value, widthValue)
  y.value = clampY(startY.value + point[1] - startClientY.value, heightValue)
  moved.value = true
  emitOffset()
}

function handleTouchEnd() : void {
  if (props.disabled) return
  moving.value = false
  if (props.adsorption) {
    const widthValue = numericSize(props.width)
    const center = x.value + widthValue / 2
    x.value = center < windowWidth.value / 2 ? leftX() : rightX(widthValue)
  }
  emitOffset()
  setTimeout(() : void => {
    moved.value = false
  }, 120)
}

function handleClick() : void {
  if (!moved.value) emit('click')
}

function handleLongPress() : void {
  emit('longpress')
}

watch(
  () : Array<any | null> | null => props.offset,
  () : void => {
    applyOffset()
  },
)

onMounted(() : void => {
  refreshWindowInfo()
  nextTick(() : void => {
    applyOffset()
  })
})

defineExpose({ applyOffset })
</script>

<style>
.i-fab {
  position: fixed;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  transition-property: left, top;
  transition-timing-function: ease-out;
  box-shadow: 0 8px 22px rgba(18, 150, 247, 0.28);
}

.i-fab__default-icon {
  color: #ffffff;
  font-size: 28px;
  font-weight: 700;
  line-height: 28px;
  text-align: center;
}

.i-fab__plus {
  position: relative;
  width: 20px;
  height: 20px;
}

.i-fab__plus-line {
  position: absolute;
  left: 1px;
  top: 9px;
  width: 18px;
  height: 2px;
  border-radius: 2px;
}

.i-fab__plus-line--vertical {
  left: 9px;
  top: 1px;
  width: 2px;
  height: 18px;
}
</style>
