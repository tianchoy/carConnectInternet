import { computed } from 'vue';
const __sfc__ = defineComponent({
    __name: 'i-badge',
    name: 'i-badge',
    props: {
        label: {
            type: String,
            default: '',
        },
        count: {
            type: Number,
            default: 0,
        },
        value: {
            type: [String, Number],
            default: '',
        },
        type: {
            type: String,
            default: 'danger',
        },
        dot: {
            type: Boolean,
            default: true,
        },
        maxCount: {
            type: Number,
            default: 99,
        },
        max: {
            type: Number,
            default: 99,
        },
        position: {
            type: String,
            default: 'right',
        },
        offset: {
            type: Array,
            default(): number[] {
                return [0, 0];
            },
        },
        fontSize: {
            type: [String, Number],
            default: '9',
        },
        showZero: {
            type: Boolean,
            default: false,
        },
        hidden: {
            type: Boolean,
            default: false,
        },
        bgColor: {
            type: String,
            default: 'error',
        },
        fontColor: {
            type: String,
            default: 'white',
        },
        color: {
            type: String,
            default: '',
        },
    },
    emits: ['click'],
    setup(__props) {
        const __ins = getCurrentInstance()!;
        const _ctx = __ins.proxy as InstanceType<typeof __sfc__>;
        const _cache = __ins.renderCache;
        /**
         * Props 说明：依据 DCloud uni-app x badge 的角标能力。
         * - label: 文本角标，优先级最高。
         * - count: 数字角标，优先级低于 label。
         * - dot: 是否显示点角标，优先级低于 label/count。
         * - maxCount: 数字大于此值时显示 +。
         * - position: 位置，支持 right、left、bottomLeft、bottomRight、top、bottom。
         * - offset: 偏移量，格式为 [x, y]。
         * - bgColor/fontColor/fontSize: 背景色、文字色、文字大小。
         * - value/type/max/color: 兼容旧版 i-badge 写法。
         */
        const props = __props;
        /**
         * Emits 说明：组件向外派发的事件，用于页面监听交互结果或同步受控值。
         * - click: 点击组件时触发，用于响应用户主动操作。
         */
        function emit(event: string, ...do_not_transform_spread: Array<any | null>) {
            __ins.emit(event, ...do_not_transform_spread);
        }
        function hasNumberValue(): boolean {
            if (props.value != null && props.value.toString().length > 0)
                return true;
            return props.count > 0 || props.showZero;
        }
        function effectiveCount(): number {
            if (props.value != null && props.value.toString().length > 0) {
                if (typeof props.value == 'number')
                    return props.value as number;
                return parseFloat(props.value as string);
            }
            return props.count;
        }
        function getMaxCount(): number {
            if (props.maxCount != 99)
                return props.maxCount;
            return props.max;
        }
        function effectiveBgColor(): string {
            if (props.bgColor.length > 0)
                return props.bgColor;
            return props.type;
        }
        function effectiveFontColor(): string {
            if (props.color.length > 0)
                return props.color;
            return props.fontColor;
        }
        function normalizeTheme(value: string): string {
            const text = value;
            if (text == 'danger')
                return 'error';
            if (text == 'error' ||
                text == 'primary' ||
                text == 'success' ||
                text == 'warning' ||
                text == 'info')
                return text;
            return 'custom';
        }
        function parseColor(value: string): string {
            const text = value;
            if (text == 'white')
                return '#ffffff';
            if (text == 'black')
                return '#000000';
            if (text == 'danger' || text == 'error')
                return '#f56c6c';
            if (text == 'primary')
                return '#3c9cff';
            if (text == 'success')
                return '#5ac725';
            if (text == 'warning')
                return '#f9ae3d';
            if (text == 'info')
                return '#909399';
            return text;
        }
        function normalizePosition(value: string): string {
            const text = value;
            if (text == 'rightTop')
                return 'right';
            if (text == 'leftTop')
                return 'left';
            if (text == 'rightBottom')
                return 'bottomRight';
            if (text == 'leftBottom')
                return 'bottomLeft';
            if (text == 'left' ||
                text == 'right' ||
                text == 'bottomLeft' ||
                text == 'bottomRight' ||
                text == 'top' ||
                text == 'bottom')
                return text;
            return 'right';
        }
        function getOffset(index: number): number {
            const offset = props.offset;
            if (offset == null || offset.length <= index)
                return 0;
            const value = offset[index] as number | string | null;
            if (typeof value == 'number')
                return value as number;
            if (typeof value == 'string')
                return parseFloat(value as string);
            return 0;
        }
        function positionStyle(): string {
            const x = getOffset(0);
            const y = getOffset(1);
            const position = normalizePosition(props.position);
            const edge = 0;
            if (position == 'left')
                return 'left:' + (edge + x) + 'px;top:' + (edge + y) + 'px;';
            if (position == 'bottomLeft')
                return 'left:' + (edge + x) + 'px;bottom:' + (edge - y) + 'px;';
            if (position == 'bottomRight')
                return 'right:' + (edge - x) + 'px;bottom:' + (edge - y) + 'px;';
            if (position == 'top')
                return 'left:50%;top:' + (edge + y) + 'px;transform:translateX(-50%);margin-left:' + x + 'px;';
            if (position == 'bottom')
                return 'left:50%;bottom:' + (edge - y) + 'px;transform:translateX(-50%);margin-left:' + x + 'px;';
            return 'right:' + (edge - x) + 'px;top:' + (edge + y) + 'px;';
        }
        function formatSize(value: string | number): string {
            const text = value.toString();
            if (text.indexOf('px') >= 0 || text.indexOf('rpx') >= 0 || text.indexOf('%') >= 0)
                return text;
            return text + 'px';
        }
        const bgColor = computed((): string => {
            return props.bgColor;
        });
        const showBadge = computed((): boolean => {
            if (props.hidden)
                return false;
            if (props.label.length > 0)
                return true;
            if (hasNumberValue()) {
                if (effectiveCount() == 0 && !props.showZero)
                    return props.dot;
                return true;
            }
            return props.dot;
        });
        const displayValue = computed((): string => {
            if (props.label.length > 0)
                return props.label;
            const value = effectiveCount();
            const max = getMaxCount();
            if (value > max)
                return max + '+';
            return value.toString(10);
        });
        const dot = computed((): boolean => {
            if (props.label.length > 0)
                return false;
            if (hasNumberValue()) {
                if (effectiveCount() == 0 && !props.showZero)
                    return props.dot;
                return false;
            }
            return props.dot;
        });
        const rootClass = computed((): string => {
            const classes = ['i-badge'];
            const position = normalizePosition(props.position);
            classes.push('i-badge--' + position);
            if (dot.value)
                classes.push('i-badge--dot');
            if (position == 'left' || position == 'bottomLeft')
                classes.push('i-badge--left-space');
            if (dot.value && (position == 'left' || position == 'bottomLeft')) {
                classes.push('i-badge--dot-left-space');
            }
            if (dot.value && (position == 'bottomLeft' || position == 'bottomRight' || position == 'bottom')) {
                classes.push('i-badge--dot-bottom');
            }
            if (dot.value && position == 'top')
                classes.push('i-badge--dot-top');
            return classes.join(' ');
        });
        const badgeClass = computed((): string => {
            const classes = ['i-badge__mark', 'i-badge__mark--' + normalizeTheme(effectiveBgColor())];
            if (dot.value)
                classes.push('i-badge__mark--dot');
            classes.push('i-badge__mark--' + normalizePosition(props.position));
            return classes.join(' ');
        });
        const badgeStyle = computed((): string => {
            let style = positionStyle();
            const bgColor = parseColor(effectiveBgColor());
            if (bgColor.length > 0)
                style = style + 'background-color:' + bgColor + ';';
            return style;
        });
        const badgeTextStyle = computed((): string => {
            return 'color:' + parseColor(effectiveFontColor()) + ';font-size:' + formatSize(props.fontSize) + ';';
        });
        function handleClick() {
            emit('click', {
                label: props.label,
                count: effectiveCount(),
                value: displayValue.value,
                dot: dot.value,
                position: props.position,
            });
        }
        return (): any | null => {
            return _cE("view", _uM({
                class: _nC(rootClass.value)
            }), [
                renderSlot(_ctx.$slots, "default"),
                isTrue(showBadge.value && dot.value)
                    ? _cE("view", _uM({
                        key: 0,
                        class: _nC(badgeClass.value),
                        style: _nS(badgeStyle.value),
                        onClick: handleClick
                    }), null, 6 /* CLASS, STYLE */)
                    : isTrue(showBadge.value)
                        ? _cE("view", _uM({
                            key: 1,
                            class: _nC(badgeClass.value),
                            style: _nS(badgeStyle.value),
                            onClick: handleClick
                        }), [
                            _cE("text", _uM({
                                class: "i-badge__text",
                                style: _nS(badgeTextStyle.value)
                            }), _tD(displayValue.value), 5 /* TEXT, STYLE */)
                        ], 6 /* CLASS, STYLE */)
                        : _cC("v-if", true)
            ], 2 /* CLASS */);
        };
    }
});
export default __sfc__;
export type IBadgeComponentPublicInstance = InstanceType<typeof __sfc__>;
const GenUniModulesIUiXComponentsIBadgeIBadgeStyles = [_uM([["i-badge", _pS(_uM([["position", "relative"], ["alignSelf", "flex-start"], ["paddingTop", 10], ["paddingRight", 12], ["overflow", "visible"]]))], ["i-badge--left-space", _pS(_uM([["paddingLeft", 12], ["paddingRight", 0]]))], ["i-badge--dot", _pS(_uM([["paddingTop", 4], ["paddingRight", 4]]))], ["i-badge--dot-left-space", _pS(_uM([["paddingLeft", 4], ["paddingRight", 0]]))], ["i-badge--bottomLeft", _pS(_uM([["paddingTop", 0], ["paddingBottom", 10]]))], ["i-badge--bottomRight", _pS(_uM([["paddingTop", 0], ["paddingBottom", 10]]))], ["i-badge--bottom", _pS(_uM([["paddingTop", 0], ["paddingBottom", 10]]))], ["i-badge--dot-bottom", _pS(_uM([["paddingBottom", 4]]))], ["i-badge--top", _pS(_uM([["paddingRight", 0]]))], ["i-badge--dot-top", _pS(_uM([["paddingRight", 0]]))], ["i-badge__mark", _pS(_uM([["position", "absolute"], ["zIndex", 2], ["minWidth", 20], ["height", 20], ["paddingTop", 0], ["paddingRight", 6], ["paddingBottom", 0], ["paddingLeft", 6], ["borderTopLeftRadius", 10], ["borderTopRightRadius", 10], ["borderBottomRightRadius", 10], ["borderBottomLeftRadius", 10], ["flexDirection", "row"], ["alignItems", "center"], ["justifyContent", "center"], ["overflow", "visible"]]))], ["i-badge__mark--dot", _pS(_uM([["width", 8], ["minWidth", 8], ["height", 8], ["paddingTop", 0], ["paddingRight", 0], ["paddingBottom", 0], ["paddingLeft", 0], ["borderTopLeftRadius", 4], ["borderTopRightRadius", 4], ["borderBottomRightRadius", 4], ["borderBottomLeftRadius", 4]]))], ["i-badge__mark--danger", _pS(_uM([["backgroundColor", "#f56c6c"]]))], ["i-badge__mark--error", _pS(_uM([["backgroundColor", "#f56c6c"]]))], ["i-badge__mark--primary", _pS(_uM([["backgroundColor", "#3c9cff"]]))], ["i-badge__mark--success", _pS(_uM([["backgroundColor", "#5ac725"]]))], ["i-badge__mark--warning", _pS(_uM([["backgroundColor", "#f9ae3d"]]))], ["i-badge__mark--info", _pS(_uM([["backgroundColor", "#909399"]]))], ["i-badge__text", _pS(_uM([["color", "#ffffff"], ["lineHeight", "20px"], ["whiteSpace", "nowrap"]]))]])];
//# sourceMappingURL=i-badge.uvue.map