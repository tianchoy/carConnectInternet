import _easycom_custom_navBar from '@/components/custom-navBar/custom-navBar.uvue';
import { ref } from 'vue';
const __sfc__ = defineComponent({
    __name: 'scancode',
    setup(__props) {
        const __ins = getCurrentInstance()!;
        const _ctx = __ins.proxy as InstanceType<typeof __sfc__>;
        const _cache = __ins.renderCache;
        const scanFunctionIsUseable = ref(true);
        const handleScan = (e: any) => {
            if (scanFunctionIsUseable.value && isTruthy(e.detail.result)) {
                uni.vibrateLong();
                scanFunctionIsUseable.value = false;
                const scanResult = e.detail.result;
                console.log('扫码结果:', scanResult, " at pages/scancode/scancode.uvue:23");
                // 获取页面栈
                const pages = getCurrentPages();
                if (pages.length >= 2) {
                    const prevPage = pages[pages.length - 2]; // 获取上一页实例
                    // 直接给上一页面的carInfo.imei赋值
                    if (prevPage != null && isTruthy(prevPage.carInfo)) {
                        prevPage.carInfo.imei = scanResult;
                        console.log('已设置上一页面IMEI:', scanResult, " at pages/scancode/scancode.uvue:33");
                    }
                    else {
                        // 如果直接赋值失败
                        uni.$emit('scanCodeResult', { result: scanResult });
                    }
                }
                uni.showToast({
                    title: '扫码成功',
                    icon: 'success',
                    duration: 1000
                });
                // 延迟返回上一页
                setTimeout(() => {
                    uni.navigateBack({
                        delta: 1
                    });
                }, 1000);
            }
        };
        const error = (e: any) => {
            console.log('摄像头错误:', e, " at pages/scancode/scancode.uvue:56");
            uni.showToast({
                title: '摄像头初始化失败',
                icon: 'none'
            });
        };
        const requestCameraPermission = () => {
            uni.getSetting({
                success: (res) => {
                    if (isTruthy(res.authSetting['scope.camera'])) {
                        console.log('已有摄像头权限', " at pages/scancode/scancode.uvue:67");
                        return;
                    }
                    uni.authorize({
                        scope: 'scope.camera',
                        success: () => {
                            console.log('摄像头权限授权成功', " at pages/scancode/scancode.uvue:74");
                        },
                        fail: () => {
                            uni.showModal({
                                title: '权限提示',
                                content: '需要摄像头权限才能扫码，是否去设置开启？',
                                success: (modalRes) => {
                                    if (modalRes.confirm) {
                                        uni.openSetting();
                                    }
                                    else {
                                        uni.navigateBack();
                                    }
                                }
                            });
                        }
                    });
                }
            });
        };
        onLoad((_options) => {
            requestCameraPermission();
        });
        return (): any | null => {
            const _component_custom_navBar = resolveEasyComponent("custom-navBar", _easycom_custom_navBar);
            const _component_camera = resolveComponent("camera");
            return _cE("view", _uM({ class: "container" }), [
                _cV(_component_custom_navBar, _uM({
                    title: "扫码添加ID",
                    "show-back": true,
                    backgroundColor: "#fff",
                    textColor: "#333",
                    showCapsule: false
                })),
                _cE("view", _uM({ class: "scancode-box" }), [
                    _cV(_component_camera, _uM({
                        "device-position": "back",
                        mode: "scanCode",
                        flash: "off",
                        onScancode: handleScan,
                        onError: error,
                        class: "scan-code"
                    }))
                ]),
                _cE("view", _uM({ class: "tip" }), "请将IMEI码放入框内,自动扫描")
            ]);
        };
    }
});
export default __sfc__;
const GenPagesScancodeScancodeStyles = [_uM([["container", _pS(_uM([["display", "flex"], ["flexDirection", "column"], ["backgroundColor", "#000000"]]))], ["scancode-box", _pS(_uM([["flexGrow", 1], ["flexShrink", 1], ["flexBasis", "0%"], ["paddingTop", 0], ["paddingRight", 0], ["paddingBottom", 0], ["paddingLeft", 0]]))], ["scan-code", _pS(_uM([["width", "100%"], ["height", "100%"]]))], ["tip", _pS(_uM([["position", "fixed"], ["bottom", "100rpx"], ["left", 0], ["right", 0], ["textAlign", "center"], ["color", "#ffffff"], ["fontSize", "28rpx"], ["backgroundColor", "rgba(0,0,0,0.5)"], ["paddingTop", "20rpx"], ["paddingRight", "20rpx"], ["paddingBottom", "20rpx"], ["paddingLeft", "20rpx"]]))]])];
//# sourceMappingURL=scancode.uvue.map