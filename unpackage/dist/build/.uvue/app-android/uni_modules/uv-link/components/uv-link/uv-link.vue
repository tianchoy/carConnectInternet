import mpMixin from '@/uni_modules/uv-ui-tools/libs/mixin/mpMixin.js';
import mixin from '@/uni_modules/uv-ui-tools/libs/mixin/mixin.js';
import props from './props.js';
/**
 * link 超链接
 * @description 该组件为超链接组件，在不同平台有不同表现形式：在APP平台会通过plus环境打开内置浏览器，在小程序中把链接复制到粘贴板，同时提示信息，在H5中通过window.open打开链接。
 * @tutorial https://www.uvui.cn/components/link.html
 * @property {String}			color		文字颜色 （默认 color['uv-primary'] ）
 * @property {String ｜ Number}	fontSize	字体大小，单位px （默认 15 ）
 * @property {Boolean}			underLine	是否显示下划线 （默认 false ）
 * @property {String}			href		跳转的链接，要带上http(s)
 * @property {String}			mpTips		各个小程序平台把链接复制到粘贴板后的提示语（默认“链接已复制，请在浏览器打开”）
 * @property {String}			lineColor	下划线颜色，默认同color参数颜色
 * @property {String}			text		超链接的问题，不使用slot形式传入，是因为nvue下无法修改颜色
 * @property {Object}			customStyle	定义需要用到的外部样式
 *
 * @example <uv-link href="http://www.uvui.cn">蜀道难，难于上青天</uv-link>
 */
const __sfc__ = defineComponent({
    name: "uv-link",
    emits: ['click'],
    mixins: [mpMixin, mixin, props],
    computed: {
        linkStyle(): UTSJSONObject {
            const style = {
                color: this.color,
                fontSize: this.$uv.addUnit(this.fontSize),
                // line-height设置为比字体大小多2px
                lineHeight: this.$uv.addUnit(this.$uv.getPx(this.fontSize) + 2),
                textDecoration: this.underLine ? 'underline' : 'none'
            } as UTSJSONObject;
            return style;
        }
    },
    methods: {
        openLink() {
            this.$emit('click');
        }
    }
});
export default __sfc__;
function GenUniModulesUvLinkComponentsUvLinkUvLinkRender(this: InstanceType<typeof __sfc__>): any | null {
    const _ctx = this;
    const _cache = this.$.renderCache;
    return _cE("text", _uM({
        class: "uv-link",
        onClick: withModifiers(_ctx.openLink, ["stop"]),
        style: _nS([_ctx.linkStyle, _ctx.$uv.addStyle(_ctx.customStyle)])
    }), _tD(_ctx.text), 13 /* TEXT, STYLE, PROPS */, ["onClick"]);
}
export type UvLinkComponentPublicInstance = InstanceType<typeof __sfc__>;
const GenUniModulesUvLinkComponentsUvLinkUvLinkStyles = [_uM([["uv-link", _pS(_uM([["lineHeight", 1], ["display", "flex"], ["flexDirection", "row"], ["flexWrap", "wrap"], ["flexGrow", 1], ["flexShrink", 1], ["flexBasis", "0%"], ["color", "#3c9cff"]]))]])];
