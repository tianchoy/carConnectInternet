import { RegisterRequest } from "../../api/request";
import _easycom_custom_navBar from '@/components/custom-navBar/custom-navBar.uvue';
import _easycom_i_input from '@/uni_modules/i-ui-x/components/i-input/i-input.uvue';
import _easycom_i_button from '@/uni_modules/i-ui-x/components/i-button/i-button.uvue';
import _easycom_app_toast from '@/components/app-toast/app-toast.uvue';
import { ref, onMounted, onUnmounted } from 'vue';
import { registerPersonalUser } from '../../api/request.uts';
import { resetTokenExpiredState } from '../../api/http.uts';
import { schedulePostLoginInitialization } from '../../services/app-startup.uts';
import { showAppToast } from '../../utils/toast.uts';
import { clearSmsRegisterContext, getSmsRegisterContext } from '../../services/auth/sms-register-context.uts';
import type { SmsRegisterContext } from '../../services/auth/sms-register-context.uts';
type PasswordForm = {
    password: string;
    confirmPassword: string;
};
const __sfc__ = defineComponent({
    __name: 'set-password',
    setup(__props) {
        const __ins = getCurrentInstance()!;
        const _ctx = __ins.proxy as InstanceType<typeof __sfc__>;
        const _cache = __ins.renderCache;
        const form = ref<PasswordForm>({
            password: '',
            confirmPassword: ''
        } as PasswordForm);
        const submitting = ref(false);
        let registerContext: SmsRegisterContext | null = null;
        const returnToLogin = (): void => {
            clearSmsRegisterContext();
            uni.reLaunch({ url: '/pages/login/login' });
        };
        const completeLogin = (token: string): void => {
            if (token == '') {
                showAppToast({ title: '注册失败，请重试', icon: 'none' } as ShowToastOptions);
                return;
            }
            uni.setStorageSync('token', token);
            resetTokenExpiredState();
            clearSmsRegisterContext();
            showAppToast({ title: '注册成功', icon: 'success' } as ShowToastOptions);
            setTimeout(() => {
                uni.reLaunch({
                    url: '/pages/index/index',
                    success: () => {
                        schedulePostLoginInitialization();
                    }
                });
            }, 500);
        };
        const isValidPassword = (): boolean => {
            const password = form.value.password;
            if (password.length < 8 || password.length > 16) {
                showAppToast({ title: '密码长度应为8至16位', icon: 'none' } as ShowToastOptions);
                return false;
            }
            let categoryCount = 0;
            if (/[0-9]/.test(password))
                categoryCount += 1;
            if (/[A-Za-z]/.test(password))
                categoryCount += 1;
            if (/[^A-Za-z0-9]/.test(password))
                categoryCount += 1;
            if (categoryCount < 2) {
                showAppToast({ title: '密码需包含至少两种字符类型', icon: 'none' } as ShowToastOptions);
                return false;
            }
            return true;
        };
        const validateForm = (): boolean => {
            if (form.value.password == '') {
                showAppToast({ title: '请设置登录密码', icon: 'none' } as ShowToastOptions);
                return false;
            }
            if (!isValidPassword())
                return false;
            if (form.value.confirmPassword == '') {
                showAppToast({ title: '请再次输入登录密码', icon: 'none' } as ShowToastOptions);
                return false;
            }
            if (form.value.password != form.value.confirmPassword) {
                showAppToast({ title: '两次输入的密码不一致', icon: 'none' } as ShowToastOptions);
                return false;
            }
            return true;
        };
        const submitRegister = async (): Promise<void> => {
            const context = registerContext;
            if (submitting.value || !validateForm())
                return;
            if (context == null) {
                showAppToast({ title: '注册信息已失效，请重新获取验证码', icon: 'none' } as ShowToastOptions);
                returnToLogin();
                return;
            }
            try {
                submitting.value = true;
                const response = await registerPersonalUser({
                    password: form.value.password,
                    confirmPassword: form.value.confirmPassword,
                    phonenumber: context.phonenumber,
                    smsCode: context.smsCode
                } as RegisterRequest);
                const token = response.data != null ? response.data.getString('access_token', '') : '';
                if (response.code == 200 && token != '') {
                    completeLogin(token);
                    return;
                }
                showAppToast({ title: response.msg != "" ? response.msg : '注册失败，请稍后重试', icon: 'none' } as ShowToastOptions);
            }
            catch (error: any) {
                showAppToast({ title: '注册失败，请检查网络后重试', icon: 'none' } as ShowToastOptions);
            }
            finally {
                submitting.value = false;
            }
        };
        onMounted(() => {
            registerContext = getSmsRegisterContext();
            if (registerContext == null) {
                showAppToast({ title: '注册信息已失效，请重新获取验证码', icon: 'none' } as ShowToastOptions);
                setTimeout(() => {
                    returnToLogin();
                }, 300);
            }
        });
        onUnmounted(() => {
            if (!submitting.value && registerContext != null)
                clearSmsRegisterContext();
        });
        return (): any | null => {
            const _component_custom_navBar = resolveEasyComponent("custom-navBar", _easycom_custom_navBar);
            const _component_i_input = resolveEasyComponent("i-input", _easycom_i_input);
            const _component_i_button = resolveEasyComponent("i-button", _easycom_i_button);
            const _component_app_toast = resolveEasyComponent("app-toast", _easycom_app_toast);
            return _cE(Fragment, null, [
                _cE("view", _uM({ class: "container" }), [
                    _cV(_component_custom_navBar, _uM({
                        title: "",
                        "show-back": true,
                        backgroundColor: "#fbfcfe",
                        textColor: "#333333",
                        showCapsule: false
                    })),
                    _cE("view", _uM({ class: "content" }), [
                        _cE("text", _uM({ class: "page-title" }), "设置登录密码"),
                        _cE("text", _uM({ class: "page-subtitle" }), "注册成功。请完成密码设置后继续。"),
                        _cV(_component_i_input, _uM({
                            modelValue: form.value.password,
                            "onUpdate:modelValue": ($event: string) => { (form.value.password) = $event; },
                            class: "password-input",
                            placeholder: "请设置登录密码",
                            password: true,
                            height: "110rpx",
                            round: "25rpx",
                            borderColor: "#d7e3ef",
                            placeholderStyle: "color:#a8b8ca;font-size:34rpx;",
                            fontSize: "34rpx",
                            color: "#333333"
                        }), null, 8 /* PROPS */, ["modelValue", "onUpdate:modelValue"]),
                        _cE("text", _uM({ class: "password-hint" }), "8–16 位，且必须包含数字、字母、特殊字符中的至少两种"),
                        _cV(_component_i_input, _uM({
                            modelValue: form.value.confirmPassword,
                            "onUpdate:modelValue": ($event: string) => { (form.value.confirmPassword) = $event; },
                            class: "password-input confirm-password-input",
                            placeholder: "请再次输入登录密码",
                            password: true,
                            height: "110rpx",
                            round: "25rpx",
                            borderColor: "#d7e3ef",
                            placeholderStyle: "color:#a8b8ca;font-size:34rpx;",
                            fontSize: "34rpx",
                            color: "#333333"
                        }), null, 8 /* PROPS */, ["modelValue", "onUpdate:modelValue"]),
                        _cV(_component_i_button, _uM({
                            class: "submit-button",
                            type: "primary",
                            block: "",
                            round: "25rpx",
                            color: "#3485df",
                            customStyle: "height:104rpx;",
                            loading: submitting.value,
                            onClick: submitRegister
                        }), _uM({
                            default: withSlotCtx((): any[] => [" 完成设置 "]),
                            _: 1 /* STABLE */
                        }), 8 /* PROPS */, ["loading"])
                    ])
                ]),
                _cV(_component_app_toast)
            ], 64 /* STABLE_FRAGMENT */);
        };
    }
});
export default __sfc__;
const GenPagesLoginSetPasswordStyles = [_uM([["container", _pS(_uM([["backgroundColor", "#fbfcfe"]]))], ["content", _pS(_uM([["paddingTop", "54rpx"], ["paddingRight", "40rpx"], ["paddingBottom", 0], ["paddingLeft", "40rpx"]]))], ["page-title", _pS(_uM([["display", "flex"], ["color", "#1f2d3d"], ["fontSize", "54rpx"], ["fontWeight", 700], ["lineHeight", "76rpx"]]))], ["page-subtitle", _pS(_uM([["display", "flex"], ["marginTop", "28rpx"], ["color", "#7f96ae"], ["fontSize", "30rpx"], ["lineHeight", "44rpx"]]))], ["password-input", _pS(_uM([["width", "100%"], ["marginTop", "62rpx"]]))], ["confirm-password-input", _pS(_uM([["marginTop", "30rpx"]]))], ["password-hint", _pS(_uM([["display", "flex"], ["marginTop", "22rpx"], ["marginRight", "6rpx"], ["marginBottom", 0], ["marginLeft", "6rpx"], ["color", "#7f96ae"], ["fontSize", "26rpx"], ["lineHeight", "40rpx"]]))], ["submit-button", _pS(_uM([["marginTop", "70rpx"]]))], ["i-input", _pS(_uM([["boxSizing", "border-box"]]))], ["i-input__field", _pS(_uM([["paddingTop", 0], ["paddingBottom", 0]]))], ["i-button__text", _pS(_uM([["fontSize", "38rpx"], ["fontWeight", 600]]))]])];
